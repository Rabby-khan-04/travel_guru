import PropTypes from "prop-types";

const BookingForm = ({ name, id }) => {
  return <form>BookingForm</form>;
};

BookingForm.propTypes = {
  name: PropTypes.string,
  id: PropTypes.string,
};

export default BookingForm;
